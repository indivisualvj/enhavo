import FormType from "@enhavo/app/Form/FormType";
import "inputmask/dist/jquery.inputmask";

export default class MaskedType extends FormType
{
    protected init()
    {
        let config = this.$element.data('inputmask');
        this.$element.inputmask(config);
    }
}
